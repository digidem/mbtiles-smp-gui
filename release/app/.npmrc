build_from_source=true
sqlite_libname=sqlite3
# For macOS, we're using a different approach with --target_arch=arm64 --fallback-to-build
# These settings are kept for non-macOS platforms
runtime=electron
target=26.6.10
disturl=https://electronjs.org/headers
